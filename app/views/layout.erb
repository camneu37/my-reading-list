<!DOCTYPE html>
<html>
  <head>
    <meta charset="UTF-8">
    <title>My Reading List</title>
  </head>
  <body>

    <div class="container">
      <h1>My Reading List</h1>

      <br>

      <% if flash.has?(:message) %>
        <%= flash[:message] %>
      <% end %>

      <%= yield %>

      <% if logged_in?%>
      <a href="/logout">Log Out</a>
      <% end %>

  </body>
</html>
